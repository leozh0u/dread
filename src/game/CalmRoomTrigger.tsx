// No component needed anymore — CALM_ROOM in triggers.ts is checked
// directly by useTriggersLoop's bounding-box test. File kept as a
// placeholder in case a visual marker (floor glow, etc.) gets added later.
export {}
